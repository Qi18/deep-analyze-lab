# Phase 5：单能力 SFT

状态：planned

## 学习目标

监督学习如何改变代码、表格理解和输出格式。

## 实验任务

从基础权重训练单能力版本，与训练前使用同协议评测。

## 验收条件

- 输入、模型、配置和执行预算已固定。
- 自动断言与人工复核均完成。
- 成功和失败轨迹全部保留。
- 结果已登记到根目录 `experiments.csv`。
- 没有把计划目标写成实测结论。

## 阶段成果

checkpoint manifest、训练曲线和分能力对照。

后续执行时在本目录补充 `notes/`、`configs/`、`scripts/` 和 `results/`；只创建实际使用的文件。
